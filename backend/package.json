{
  "name": "web-portfolio-backend",
  "private": true,
  "scripts": {
    "start": "py -3.11 -m uvicorn server:app --host 0.0.0.0 --port 8000",
    "dev": "py -3.11 -m uvicorn server:app --host 0.0.0.0 --port 8000 --reload"
  },
  "dependencies": {
    "fastapi": "^0.0.8"
  }
}
